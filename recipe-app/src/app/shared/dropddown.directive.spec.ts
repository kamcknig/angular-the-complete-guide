import { DropdownDirective } from './dropddown.directive';

describe('DropddownDirective', () => {
  it('should create an instance', () => {
    const directive = new DropdownDirective();
    expect(directive).toBeTruthy();
  });
});
